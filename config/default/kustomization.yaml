apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: build-operator-system

resources:
  - ../crd
  - ../rbac
  - ../manager
  # - ../webhook  # uncomment when cert-manager available

images:
  - name: controller
    newName: ghcr.io/builderhub/build-operator
    newTag: latest
